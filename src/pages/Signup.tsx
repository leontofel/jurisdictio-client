import SignupForm from '@components/global/SignupForm';

export default function SignupPage () {
  return (
      <div className="flex flex-col items-center justify-center flex-grow bg-gray-100 py-12">
        <SignupForm />
      </div>
  );
}
